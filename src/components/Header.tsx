import React from 'react';
import { IonHeader, IonToolbar, IonTitle } from '@ionic/react';

const Header: React.FC = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>Рудих Антон КН-31</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default Header;